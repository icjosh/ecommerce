cart = $("#cart");
cart.html(
    '<%= escape_javascript(render(:partial => "cart", :object => @cart)) %>');
$("#flash_notice").append("<%= escape_javascript(flash.delete(:notice)) %>");
$("#cart_actions").slideUp("faster");
cart.slideUp("faster");
